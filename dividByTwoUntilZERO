#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <stdbool.h>


int main()
{
    int n;
    scanf("%d", &n);
    while (n % 2 == 0) 
          n /= 2;
    printf("%d\n", n); 
    return 0;
}
